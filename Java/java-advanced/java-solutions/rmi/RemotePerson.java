package rmi;

public class RemotePerson extends AbstractPerson {
    public RemotePerson(String passportId, String firstName, String lastName) {
        super(passportId, firstName, lastName);
    }
}
