Для отношнения между Event и Tag сделано many to many отношение, чтобы в дальнейшем была возможность сделать
Tag более сложной сущностью. Например, добавить к тег поля Color и Label. Color отвечал бы за то, каким
цветом будет отображаться Event в календаре, а Label отвечал бы за то, какая иконка будет отображаться радом
с названием Event. При текущим дизайне для подобного расширения необходимо сделать следующее:
1) Схлопнуть в таблице tags все теги с одинаковыми именами и соответствующим образом изменить таблицу
events_tags.
2) В таблице tags сделать name уникальным.
3) Добавить в таблицу tags два nullable столбца: Color и Label.
4) Добавить роут для создания Tag без Event.
5) Добавить роут для изменения Tag.
6) Добавить роут для получения всех Tag.
7) Изменить создавние Event:
    а) Если имя Tag при создании Event существует в бд, то добавить запись в таблицу events_tags.
    б) Если имя Tag при создании Event не существует в бд, то создать Tag без Color и Label и
    добавить запись в таблицу events_tags.

Понять, какие файлы не нужны и убрать их. Пофиксить pipeline, понять, почему он вообще запускается